# Secrets folder
# 
# This folder contains sensitive files that should NOT be committed to git.
#
# Required files (see .example files for templates):
# - api_key.txt: API authentication key
# - openrouter_api_key.txt: OpenRouter API key (one line, no quotes)
# - credentials.json: Google Service Account JSON key file
#
# Setup instructions: See INSTALL.md
#
# Template files (*.example) are tracked in git for reference.
# Actual secret files (*.txt, *.json) are ignored by .gitignore.
