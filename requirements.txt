# Single dependency set used by all production containers.
#
# Dev/test tooling stays in `requirements-dev.txt`.
#
# Notes:
# - We want the latest OpenAI SDK (`openai>=2`). The PDF extractor stack lives
#   in `requirements-pdf.txt` and is installed only in `Dockerfile.scraper-pdf`.
requests>=2.32.5
pandas>=3.0.0
openpyxl>=3.1.5
flask>=3.1.2
flasgger>=0.9.7.1
google-auth>=2.48.0
google-api-python-client>=2.188.0
yoyo-migrations>=9.0.0

# AI / PDF parsing
pydantic-ai-slim[openai]==1.102.0
openai>=2.29.0
